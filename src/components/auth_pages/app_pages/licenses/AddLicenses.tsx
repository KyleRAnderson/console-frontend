import React from 'react';
import Hunt from '../../../../models/Hunt';

export type Props = {
    hunt: Hunt;
};

/**
 * Component for creating licenses for participants in a given hunt.
 */
export default function AddLicenses(props: Props): JSX.Element {
    return <></>;
}
